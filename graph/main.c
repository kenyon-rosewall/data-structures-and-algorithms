#include <stdio.h>
#include <stdlib.h>
#include "types.h"
#include "graph.h"

int main()
{
  // int node_count = 5;
  // graph* g = graph_create(node_count);
  // add_edge(g, 0, 1, 1);
  // add_edge(g, 0, 2, 1);
  // add_edge(g, 0, 0, 1);
  // add_edge(g, 1, 2, 1);
  // add_edge(g, 1, 3, 1);
  // add_edge(g, 3, 4, 1);
  // add_edge(g, 4, 1, 1);
  // add_edge(g, 4, 0, 1);

  // // print_graph(g);

  // u32* n = neighbors(g, 0);
  // for (
  //   u32 i = 0;
  //   i < get_number_of_nodes(g);
  //   ++i
  // )
  // {
  //   if (n[i] == 1)
  //   {
  //     printf("%d\n", i);
  //   }
  // }
  // free(n);

  // destroy_graph(g);

  return 0;
}